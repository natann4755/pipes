import { D4Directive } from './d4.directive';

describe('D4Directive', () => {
  it('should create an instance', () => {
    const directive = new D4Directive();
    expect(directive).toBeTruthy();
  });
});
