import { D7Directive } from './d7.directive';

describe('D7Directive', () => {
  it('should create an instance', () => {
    const directive = new D7Directive();
    expect(directive).toBeTruthy();
  });
});
