import { D8Directive } from './d8.directive';

describe('D8Directive', () => {
  it('should create an instance', () => {
    const directive = new D8Directive();
    expect(directive).toBeTruthy();
  });
});
