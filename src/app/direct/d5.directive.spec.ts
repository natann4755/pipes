import { D5Directive } from './d5.directive';

describe('D5Directive', () => {
  it('should create an instance', () => {
    const directive = new D5Directive();
    expect(directive).toBeTruthy();
  });
});
