import { D6Directive } from './d6.directive';

describe('D6Directive', () => {
  it('should create an instance', () => {
    const directive = new D6Directive();
    expect(directive).toBeTruthy();
  });
});
